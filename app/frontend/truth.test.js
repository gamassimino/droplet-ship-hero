// a simple test to ensure Jest is working

describe("boolean values", () => {
  it("checks that true is true", () => {
    expect(true).toBe(true);
  });

  it("checks that false is false", () => {
    expect(false).toBe(false);
  });
});
